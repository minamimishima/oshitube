<div class="category text-center">
  <% if categories.present? %>
    <% categories.each do |category| %>
      <%= link_to category.name, category_path(category), class: "category__button btn mx-1" %>
    <% end %>
  <% end %>
</div>
